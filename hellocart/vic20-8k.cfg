MEMORY {
    HEADER: start = $A000, size = $09, file = %O, fill = yes;
    ROM: start = $A009, size = $1FF7, file = %O, fill = yes;
}
SEGMENTS {
    HEADER: load = HEADER, type = ro;
    CODE: load = ROM, type = ro;
}